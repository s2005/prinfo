# Commit Export Request

User request:

> can we improve prinfo to get a list of commits from PR and save files per
> each commit, where commit will be a folder name?

Follow-up request:

> would you mind to document this task in docs/tasks/ folder ?
