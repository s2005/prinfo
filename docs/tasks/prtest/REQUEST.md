# PR Self-Test Request

User request:

> it would be good to create PR with some linter checks running in GHA and
> then test prinfo on its own repo . save it in docs/tasks/prtest folder and
> start implementing
